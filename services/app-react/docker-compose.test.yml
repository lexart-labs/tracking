version: '3.8'

services:
  test-app:
    image: node:20
    working_dir: /app
    # Montamos todo el directorio actual en /app dentro del contenedor
    volumes:
      - .:/app
    # Aseguramos que instale dependencias si es necesario y corra los tests
    command: sh -c "npm install && npm run test"
